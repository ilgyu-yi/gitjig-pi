# Structured machine-record publication contract (#319)

- Settle a fail-closed `machineRecord` publication mode that preserves parsed JSON values without weakening egress secret scanning or actionable-reference neutralization.
- Require deterministic inert wire bytes, one publishing send, destination-bound exact reread, and no automatic retry after post-spawn ambiguity.
- Preserve existing review-record bytes as the sole documented legacy plain-body exception; runtime implementation derives separately.
