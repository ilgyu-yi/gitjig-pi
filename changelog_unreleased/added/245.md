- Added the on-demand `/authoring-brief <closed-json>` command, which injects path-routed canonical clauses into the current session before editing and replaces the local post-authoring hook layout. Its required JSON fields are `plan`, `failingCheck.description`, `failingCheck.command`, and repository-relative `paths`; malformed, unrouted, conflicting, or unresolved input is advisory-incomplete and claims no readiness. Plan and check text is untrusted session context, not executed input or routing authority. (#245)
