- The adopter install path gains its composition half: the shell can now derive its own substrate set by walking the namespaces SPEC §4.1 states, resolve where each member would land in an adopting repository, and decide per member whether to land, no-op on identical bytes, skip a differing pre-existing asset with a warning, or refuse — refusing anything whose destination normalizes outside those namespaces or whose destination container is a symbolic link. The composition writes nothing; delivery into an adopting repository is a named seam that lands separately. (#116)
