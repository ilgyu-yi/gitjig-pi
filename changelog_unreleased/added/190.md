- SPEC §3.3's `merge-review` row gains its tier-3 instrument: a CI job evaluates, on every pull request to `main` or a maintenance branch, whether a complete and adjudicated review record is pinned to the head under review — refusing a record relayed inside another comment, one pinned to a different head, an incomplete panel, and a non-empty finding bundle carrying no Judge adjudication, and failing closed on every lookup failure. It is advisory to start (§3.6's born-advisory face): it reports and does not block, and hardening it into a required check stays the operator's act. (#190)
