- A pull request now gets an advisory report of development provenance on its added lines — sentences that describe how the repository got here rather than what the current contract is — with SPEC §2.5's erasure test as the remedy on each. It blocks nothing. (#70)
