- Five load-bearing elements of the egress neutralizer's wrap patterns — the URL form's unencrypted-scheme spelling, its pull-request alternation, its backtick exclusion, and the cross-repository form's leading word boundary and digit quantifier — are each pinned by an arm whose input violates that element and nothing else; the platform-actionability of the two wrong-allow shapes was measured through the markdown render API. (#140)
