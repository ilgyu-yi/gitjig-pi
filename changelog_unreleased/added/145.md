- The verification suite now runs as a CI gate on every pull request: the `suite` required-check context joins SPEC §3.2/§4.3's recorded set, `.github/workflows/suite.yml` runs the canonical quoted-glob invocation with its install posture stated in the file, and the fail-posture inventory gains the gate's rows (#145)
