- Two new CI gates, `source-style` (formatter, linter and import assist, warnings fatal) and `type-check` (`tsc --noEmit`), running from a development-and-CI-only root manifest that never enters the substrate an adopting repository receives. (#135)
