- The local `commit-msg` hook now prints an authoring pass: the staged diff, the prepared message, the provenance reader's report over that diff, and the delete-only disposition for anything it flags. It is report-only and refuses nothing. (#218)
