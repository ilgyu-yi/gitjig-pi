- The fail-posture inventory is now machine-checked for completeness as well as for having one home: a structural check walks the local git-hook tier, collects every declared fail-open cause — both `audit_log warn` record constants and the staged-secret scan's per-cause disarm literals — and fails when one has no inventory row naming both the cause and the source that emits it. Four failure shapes the shipped scanner declared and the inventory omitted now carry rows: an unresolvable repository top at scan time, an unresolvable staged-diff base, a failed staged-path enumeration, and a present-but-unreadable `.shellsecretignore`. (#112)
