- The fail-posture inventory is now machine-checked for completeness as well as for having one home: a structural check walks the local git-hook tier, collects every declared fail-open cause — both `audit_log warn` record constants and the staged-secret scan's per-cause disarm literals — and fails when one has no row in `.pi/extensions/gitjig/postures.ts`. Five failure shapes the shipped scanner declared and the inventory omitted now carry rows: an unresolvable repository top at scan time, an unresolvable staged-diff base, both staged-path enumeration failures, and a present-but-unreadable `.shellsecretignore`. (#112)
