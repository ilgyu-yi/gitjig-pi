- §1.4 cross-review-repair history is now an instrument: the repair history is assembled from the durable review records (never author-kept), a coarse deterministic trigger fires on consecutive repair states, and the diagnosis dispatch admission is fail-closed with a deterministic consumer of its taxonomy value and invalidation finding. (#186)
