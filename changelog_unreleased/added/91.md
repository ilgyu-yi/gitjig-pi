- The command spine: `work-on` registered as a prompt template and `review`/`ship` as extension commands under `.pi/`, each on the surface the command layer's four-rung rule returns for it — `review` dispatching a delegate round-trip as a call site of the one dispatcher, and `ship` evaluating the caller-side `ac-closeout` fact while naming the platform-held AC state and every landing decision as offline undecidables in the registration record. (#91)
