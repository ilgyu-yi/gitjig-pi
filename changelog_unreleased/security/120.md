- The egress gate now reaches every publication the shell performs, not comments alone: creating an issue or pull request and editing either body go through the same secret scan and mention neutralization that issue and PR comments already did. Previously those paths published unscanned, and the class's own backstop is structurally unavailable, so nothing caught them. A created surface's title is scanned as published text in its own right, and is refused if it is blank, option-shaped, or carries a newline or NUL. (#120)
