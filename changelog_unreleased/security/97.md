- A delegate's summary and a published comment URL can no longer forge a verdict line or land a raw control byte on the tool-result surface: both now cross through the shared escaper, one line and delimited. (#97)
