- The push gate's own repairs are pinned rather than trusted: the source-time discard of an inherited derivation cache, the hook-local terminal-prompt guard, and the one-record-per-run shape of the disarmed-gate warning each now have an arm whose named mutant was demonstrated to kill it, where before each could regress silently. `githook_block`'s audit subshell reads stdin from `/dev/null` like every other child on the hook path, so it cannot consume the ref lines the pre-push adapter iterates. Two of §3.3's residual enumerations are widened in place: the accepted second-connection class names the ssh-passphrase re-prompt that `GIT_TERMINAL_PROMPT=0` does not reach, and the unmodeled set names Unicode normalization pairs beside case-pairs. (#63)
