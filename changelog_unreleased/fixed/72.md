- The warning-surface escaping lock decides what it covers from a recursive walk of `.pi/extensions/` instead of a hand-kept roster, so a module can no longer join the tree uncovered because nobody remembered to list it. Every walked file must be in one of two states — on the roster, where its interpolations are scanned, or off it with the reason recorded in that module's own header, which the lock reads rather than keeping a second list that could drift; a file in neither state fails. The walk found fourteen modules previously undecided, including the entry file beside the extension directory and every module under `commands/`, `dispatch/` and `publish/`; each is now dispositioned, and `quote.ts` is the one recorded exemption because escaping the escaper is circular. (#72)
