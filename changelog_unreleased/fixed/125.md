- The bind advisory's TTL debounce is now scoped to the repository it classified, so a session in one repository no longer suppresses another's degraded-binding advisory for the whole TTL window. (#125)
