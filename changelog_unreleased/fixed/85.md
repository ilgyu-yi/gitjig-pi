- The publish executor's bound now reaches the child's whole process group, so a `gh`-spawned grandchild dies with it instead of surviving to hold the pipes the bound was meant to close, and a child that SIGKILL cannot reap no longer wedges the tool call: an outer backstop settles it on the bound-exceeded cause. The repair that keeps an in-bound exit behind an orphan-held pipe from being refused as bound-exceeded — a withholding claimed for a send that succeeded — is now staged by an arm that reds when it is reverted, where before reverting it left the suite green. (#85)
