- The commit-msg hook no longer approves a commit whose subject is a leading comment line: it checks the message's first non-blank line, which is the line that lands under a comment-preserving cleanup. (#58)
