- A composed review round now re-sends a dispatch once, unchanged, when the delegated run reports failure, so the one transport failure measured as transient no longer costs a whole review slot. (#220)
