- A composed review round now re-sends a dispatch once, unchanged, when the delegated run reports failure, so a transient delegated-run failure need not cost a panel or Judge slot; a failure that repeats is still refused to the caller. (#220)
