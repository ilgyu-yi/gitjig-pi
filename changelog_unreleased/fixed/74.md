- The bind instrument's exclusion re-ask now names a lookup that reports which rule decides the path, and the index-clearing act for the one cause no exclude-file edit can fix, instead of prescribing a re-run with nothing to change between runs. (#74)
