- The `merge-review` gate's platform reads are retried with backoff and bounded by a per-attempt timeout, so a transient error no longer renders a refusal indistinguishable from a genuinely missing review record. (#194)
