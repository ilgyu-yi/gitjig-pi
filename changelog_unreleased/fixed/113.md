- The local tier's protected-branch commit check no longer allows silently when `HEAD` is on no branch: the adapter observes `current_branch`'s failure instead of comparing its empty output against the protected identity, and a commit with no branch subject — every commit a rebase replays — leaves one audit record naming the arm as not evaluated, so it no longer reads as an ordinary allow. (#113)
