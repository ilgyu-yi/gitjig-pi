- The egress executor's exit-inside-the-flush-grace race is proven at an injected-bounds seam (`ChildBounds`, a function argument, never an environment read) with 1.5s+ margins, instead of riding a 1s margin inside a 2s real-time window that machine load consumed and flaked; the session-level orphan-late arm keeps the grace-path publish claim with its child exiting early against the production bound. (#119)
