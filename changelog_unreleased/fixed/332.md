# One-use autonomous history recovery

Phase A adds the shared state-domain recovery substrate and wires the existing history diagnosis checkpoint at both review-round timings. It derives the sole change key from complete platform-attested identities, claims one direct v2 allowance leaf durably before independent work, records bounded profile attempts, and returns only closed invalidation-selected routes. Handoff mode and all ready, merge, authorization, plan-selection, and finding-escalation boundaries remain unchanged.
