- A pull request body composed through `gitjig_publish` now keeps the `Closes #N` first line SPEC §1.1 requires, instead of publishing it inert, and every confirmed publish that does make a reference inert reports how many it changed. (#139)
