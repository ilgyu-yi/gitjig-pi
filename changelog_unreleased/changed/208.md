- The review record's five dispositions now have one home: `record.ts` exports the enforcing list and the Resolver's `Disposition` type is derived from it, replacing a hand-spelled union that could diverge from the list deciding whether a record parses. (#208)
