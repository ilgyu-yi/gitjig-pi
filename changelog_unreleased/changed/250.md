- Replace the pre-settlement adopter composer exports with the external-adoption composition core: one exact four-disposition classifier derives a checked membership snapshot as development/CI evidence, while only handed-over and carried candidates enter the pin-v1 payload; source-only and instance-state candidates remain snapshot-only. Existing project assets still declare source-only, so the handed-over projection is empty until that declaration is removed under its later contract. The side-effect-free old/new-manifest planner consumes the pin with whole-phase refusal. This is an intentional API replacement rather than a compatibility-preserving extension; later delivery and provisioning consumers must adopt the new typed plan under their revised contracts. (#250)
