- Define the final-form external-adoption boundary before implementation: reviewed handed-over assets and an immutable manifest pin in project history, with the runtime provisioned and verified per clone.
