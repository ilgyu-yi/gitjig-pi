- Review delegates now receive the caller-side transport facts needed for a readable bounded return: the no-remote provisioned-tree fact, the unreachability of a platform-only living surface such as the PR body unless the caller embeds it in the brief, work-completed `ok` semantics, the summary bound and complete-summary requirement, and the write-early/overwrite recipe. (#230)
