- SPEC §1.9's finding judgment gains a harm-direction axis beside the nit discriminator: the Judge explicitly rules every confirmed finding `fail-closed` or `live-harm`, with no silence default, and the deterministic Resolver may defer a confirmed substantive `fail-closed` finding only when it sits on no acceptance criterion of a non-empty caller-derived manifest. (#142)
