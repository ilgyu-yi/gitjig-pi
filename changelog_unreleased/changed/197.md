- Composed reviewer and Judge briefs now carry the provisioned-tree hygiene ledger — `npm ci` first with its reason, private `mktemp -d` scratch, the `git archive` and git-state bans, and the serial re-run rule — so every dispatched delegate is told the facts of its world by the instrument instead of by hand. (#197)
