- Composed reviewer and Judge briefs now carry the provisioned-tree hygiene ledger — `npm ci` first with its reason, no local `main` ref so a comparison runs against the branch's parent commit, private `mktemp -d` scratch, the `git archive` and git-state bans, and the serial re-run rule — so a delegate dispatched through a composed review brief is told the facts of its world by the instrument instead of by hand. (#197)
